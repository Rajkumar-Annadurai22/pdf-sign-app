# PDF Sign App

A mobile-friendly web app where the user uploads a PDF, sends it to a mock server for signing, and then views the signed PDF on their device:

## Run locally

```bash
npm install
npm run dev
```
